include(file = "Configuration.json");

forEach(property="table", source=getTables(), connection="${Connection}") 
 {
    copyTable(
        source = table(name = "${table.name}"),
        target = file(name = "${table.name}.csv")
    )
}
